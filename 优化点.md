# 系统性能优化方案

## 背景

当前系统在从 Ganache/数据库迁移至 DevLeChain/区块链环境后, 出现了严重的性能问题, 主要表现为启动缓慢, 运行时持续卡顿, 以及用户交互时界面长时间无响应 (假死).

经过深入分析, 问题根源并非业务逻辑错误, 而是应用的技术实现架构无法适应真实区块链环境的延迟和资源开销. 本文档旨在提供一套清晰, 可执行的优化方案, 从根本上解决这些性能瓶颈.

---

## 核心优化方案

我们识别出三个主要的性能瓶颈, 并针对性地提出解决方案.

### 方案一: AI模型懒加载 (Lazy-Loading)

**问题:** 应用在启动时会"贪婪地"加载一个约700MB的AI推理数据文件, 导致启动时间过长, 并在初期耗尽大量内存资源.

**目标:** 避免在应用启动时加载大量数据, 将其改为在首次需要时再加载, 大幅缩短启动时间并降低初始内存占用.

**具体步骤:**

1.  **修改 `backend/app/services.py`:**
    *   改造 `get_threat_model` 函数, 使其在创建 `HierarchicalPredictor` 实例时, **不** 立刻加载推理数据.
2.  **修改 `backend/assets/model_package/predictor.py`:**
    *   在 `HierarchicalPredictor` 类的 `__init__` 方法中, 添加一个状态标记, 如 `self.data_loaded = False`.
    *   修改 `predict` 方法 (或其兼容层方法 `simulate_attack_detection`), 在其开头进行检查. 如果数据尚未加载 (`if not self.data_loaded`), 则执行数据加载流程, 完成后设置 `self.data_loaded = True`.

**预期效果:**
*   **立竿见影:** 应用启动时间预计将缩短90%以上 (从几十秒到几秒).
*   **资源释放:** 启动时的内存占用将减少约700MB.
*   **平滑过渡:** 只有在系统首次进行AI推理时, 会有一次性的加载延迟.

---

### 方案二: 后端缓存与API聚合

**问题:** 前端多个视图 (Dashboard, Threats等) 都在以高频率 (5-10秒) 并发地轮询后端接口, 这些接口又会触发对区块链节点大量重复的RPC调用, 导致CPU持续高负载.

**目标:** 大幅减少前端对后端的无效请求和对区块链的重复RPC调用, 根治系统在闲置状态下的持续性卡顿.

**具体步骤:**

1.  **后端改造 (缓存与聚合):**
    *   在 `backend/main.py` 中创建一个新的聚合API端点, 例如 `/api/system/overview`.
    *   为这个新API实现一个简单的内存缓存机制 (例如, 使用一个全局字典和时间戳), 缓存有效期设置为30-60秒.
    *   当此API被调用时, 如果缓存有效, 则直接返回缓存数据; 如果缓存过期, 则执行一次完整的状态获取 (账户信息, 提案等), 更新缓存, 然后返回新数据.
2.  **前端改造 (统一数据源):**
    *   移除所有视图组件中各自的 `setInterval` 定时器.
    *   创建一个中央数据管理服务, 设置 **唯一的一个** 定时器 (例如30秒一次), 专门调用新的 `/api/system/overview` 接口.
    *   所有相关组件都从这个中央服务响应式地获取数据, 而不是自行请求.

**预期效果:**
*   **大幅减负:** 前后端之间的API请求数量将减少90%以上, 后端CPU占用率显著下降.
*   **消除闲置卡顿:** 系统在无操作的浏览状态下将变得流畅.

---

### 方案三: 异步后台任务

**问题:** 用户在前端执行签名, 创建提案等操作时, 后端API会同步等待区块链的写入操作完成, 这个过程长达10秒以上, 导致前端界面完全冻结.

**目标:** 将所有耗时的区块链写入操作放入后台执行, 使API能够立即响应, 彻底解决用户交互时的"假死"现象.

**具体步骤:**

1.  **修改 `backend/main.py`:**
    *   在所有执行区块链写入操作的API端点函数签名中, 注入FastAPI的 `BackgroundTasks`.
2.  **修改 `backend/app/services.py`:**
    *   将服务层中的长耗时逻辑 (如 `sign_proposal` 的核心部分) 封装到一个独立的函数中.
    *   在API端点中, 调用 `background_tasks.add_task()` 来安排这个函数在后台执行.
    *   **必须删除** `_create_auto_proposal` 方法中的 `blockchain_ready.wait(timeout=10)` 阻塞代码.
3.  **立即返回响应:**
    *   API端点在添加完后台任务后, 应立即返回一个确认信息, 例如 `{"status": "processing", "message": "任务已在后台处理"}`.

**预期效果:**
*   **革命性体验提升:** 用户点击操作后, UI不再冻结, 得到即时反馈.
*   **提高系统吞吐量:** API工作线程不会被长时间占用, 能够处理更多并发请求.

---

## 总结与执行建议

以上三个方案从根本上解决了系统的性能问题. 为了获得最大且最快的收效, 建议按以下优先级执行:

🥇 **第一名 (效果最好): 方案一: AI模型懒加载**
*   **理由:** 这是 **投入产出比最高** 的方案. 改动范围最小, 效果却极其明显, 完美符合"立竿见影"的要求.

🥈 **第二名: 方案三: 异步后台任务**
*   **理由:** 这是 **提升交互体验最关键** 的方案. 它能解决用户最直观的痛点——点击后的长时间等待.

🥉 **第三名: 方案二: 后端缓存与API聚合**
*   **理由:** 这是 **最能提升系统长期稳定性** 的方案, 但因涉及前后端协同修改, **改动范围最大**, 实施起来相对复杂.

**建议首先从【方案一】入手, 您将立刻获得一个启动飞快的应用, 为后续优化打下良好基础.**
